declared-services:
  swift-enterprise-demo-alert:
    label: alertnotification
    plan: authorizedusers
applications:
- name: swift-enterprise-demo
  memory: 256M
  instances: 1
  random-route: true
  disk_quota: 1024M
  command: swift-enterprise-demo
  buildpack: swift_buildpack
  services:
  - swift-enterprise-demo-alert
