declared-services:
  swift-enterprise-demo-alert:
    label: alertnotification
    plan: authorizedusers
applications:
- name: SwiftEnterpriseDemo
  memory: 256M
  instances: 1
  random-route: true
  disk_quota: 1024M
  command: DemoAppServer
  buildpack: swift_buildpack
  services:
  - swift-enterprise-demo-alert
